# Kế hoạch triển khai: Mở rộng 30 nhóm — Giai đoạn 0, 1, 2

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dựng xong hạ tầng 7 chương / 30 nhóm, thêm Phần 7 "Dự án thực hành" vào khung bài học, viết đặc tả 7 MVP và 2 bài mẫu, để 18 bài còn lại chỉ còn là việc lặp khuôn.

**Architecture:** `src/lesson/parts.js` trở thành nguồn sự thật duy nhất cho cả khung 8 mục lẫn cấu trúc 7 chương. `src/data/nav.js` không còn viết tay mà suy ra từ `CHAPTERS`. Bài chưa viết được đánh cờ `ready: false` và hiển thị mờ trên sidebar, nên test luôn có nghĩa và người dùng nhìn thấy lộ trình. Dự án thực hành có hai cỡ: `project` nằm trong dữ liệu từng bài, `capstone` nằm ở `src/data/capstones/` theo chương.

**Tech Stack:** Vue 3 + Vite, Vitest 3 + jsdom, không backend.

**Spec:** `docs/superpowers/specs/2026-08-09-mo-rong-30-nhom-design.md`

## Global Constraints

- Tiếng Việt, xưng "bạn", giọng thân thiện, giống hệt 10 bài hiện có.
- Không dùng chữ nghiêng ở bất kỳ đâu. Không gỡ `font-style: normal` trong `src/style.css`.
- Code mẫu là C++, đặt trong `<pre v-pre><code>`, escape `<` thành `&lt;`, `&` thành `&amp;`.
- Mỗi section giữ nguyên `id`, `class="day-section"`, `data-sid`, `v-show="active"`, `defineProps({ active: Boolean })`.
- Không đổi `sid` của 10 bài cũ: `quay-lui-xau-nhi-phan`, `to-hop`, `tham-lam`, `qhd-nen-tang`, `qhd-lis-lcs-doixung`, `ngan-xep-hang-doi`, `dfs-bfs`, `dsu`, `cay-nhi-phan-bst`, `bst-nang-cao`.
- Không đặt `max-width` riêng cho `.content-row`. Bề ngang khung trang chỉ đổi qua `--shell`.
- Đúng hai mốc responsive: `1023px` và `900px`. Không thêm mốc lẻ.
- Mọi cặp màu chữ/nền đạt AA 4.5:1. Mục menu tối thiểu 44px chiều cao.
- Toàn bộ Giai đoạn 0–2 (Task 1–12) chạy bằng **Opus, inline**. Đổi sang Sonnet chỉ sau khi Task 12 xong.
- Lệnh test: `npm run test -- --run`. Lệnh build: `npm run build`.

---

## Sai lệch có chủ ý so với spec

Spec mục 6.1 nói `capstone` nằm trong dữ liệu bài cuối mỗi chương. Kế hoạch này đưa nó ra `src/data/capstones/<chapter-key>.js` vì hai lý do:

1. MVP Chương 1 (`bench`) cần viết ngay ở Giai đoạn 2, nhưng bài cuối Chương 1 (`danh-sach-lien-ket`) mãi tới Giai đoạn 3 mới viết. Gắn vào bài sẽ chặn tiến độ.
2. MVP thuộc về cả chương, không thuộc về một bài. Đặt trong dữ liệu một bài là gán sai chủ sở hữu.

Hiển thị không đổi: MVP vẫn hiện trong Phần 7 của bài cuối chương. Task 12 cập nhật lại spec cho khớp.

---

## Cấu trúc file

| File | Trách nhiệm | Task |
|---|---|---|
| `src/lesson/parts.js` | Khung 8 mục + `CHAPTERS` + suy ra `LESSON_SECTIONS` | 1, 4 |
| `src/data/nav.js` | Suy ra menu trái từ `CHAPTERS`, không viết tay | 2 |
| `src/App.vue` | Render sidebar theo chương, bài chưa viết dạng mờ | 3, 10, 11 |
| `src/style.css` | Lớp `.sb-soon` cho bài chưa viết; CSS `ProjectBrief` | 3, 5 |
| `src/components/ProjectBrief.vue` | Hiển thị `project` và `capstone`, hai chế độ | 5 |
| `src/data/capstones/index.js` | Gom 7 MVP theo khoá chương | 9 |
| `src/data/capstones/nen-mong.js` | MVP Chương 1: `core` + `bench` | 9 |
| `src/data/lessons/do-phuc-tap.js` | Dữ liệu bài mẫu 1 | 10 |
| `src/sections/DoPhucTap.vue` | Section bài mẫu 1 | 10 |
| `src/data/lessons/bang-bam.js` | Dữ liệu bài mẫu 2 | 11 |
| `src/sections/BangBam.vue` | Section bài mẫu 2 | 11 |
| `tests/parts.spec.js` | Ép cấu trúc `CHAPTERS` | 1, 4 |
| `tests/nav.spec.js` | Ép `nav.js` khớp `CHAPTERS` | 2 |
| `tests/lesson-data.spec.js` | Ép schema `project`, cờ `recall` | 6, 7 |
| `tests/capstone.spec.js` | Ép schema `capstone` và ràng buộc `reuses` | 9 |
| `docs/superpowers/specs/2026-08-09-dac-ta-7-mvp.md` | Đặc tả đầy đủ 7 MVP | 8 |

---

## Task 1: `CHAPTERS` trong `parts.js`

**Files:**
- Modify: `src/lesson/parts.js:25-38`
- Test: `tests/parts.spec.js`

**Interfaces:**
- Produces: `CHAPTERS` — mảng `{ key, num, title, capstoneReady, lessons: [{ sid, title, ready, duAn }] }`.
- Produces: `LESSON_SECTIONS` — suy ra từ `CHAPTERS`, mỗi phần tử `{ sid, title, file, chapter, ready, duAn }`. Trường `file` sinh tự động từ `sid` nên không còn viết tay.
- Produces: `sidToFile(sid)` — `'do-phuc-tap'` → `'src/sections/DoPhucTap.vue'`.

- [ ] **Step 1: Viết test thất bại**

Thêm vào cuối `tests/parts.spec.js`:

```js
import { CHAPTERS, LESSON_SECTIONS, sidToFile } from '../src/lesson/parts.js'

describe('CHAPTERS — cấu trúc 7 chương', () => {
  it('có đúng 7 chương, đánh số 1..7 liên tục', () => {
    expect(CHAPTERS).toHaveLength(7)
    expect(CHAPTERS.map(c => c.num)).toEqual([1, 2, 3, 4, 5, 6, 7])
  })

  it('có đúng 30 bài, không sid nào trùng', () => {
    const sids = CHAPTERS.flatMap(c => c.lessons.map(l => l.sid))
    expect(sids).toHaveLength(30)
    expect(new Set(sids).size).toBe(30)
  })

  it('giữ nguyên 10 sid cũ', () => {
    const sids = CHAPTERS.flatMap(c => c.lessons.map(l => l.sid))
    for (const old of [
      'quay-lui-xau-nhi-phan', 'to-hop', 'tham-lam', 'qhd-nen-tang',
      'qhd-lis-lcs-doixung', 'ngan-xep-hang-doi', 'dfs-bfs', 'dsu',
      'cay-nhi-phan-bst', 'bst-nang-cao',
    ]) {
      expect(sids).toContain(old)
    }
  })

  it('mọi bài đều khai báo đủ cờ ready và duAn', () => {
    for (const c of CHAPTERS) {
      for (const l of c.lessons) {
        expect(typeof l.ready).toBe('boolean')
        expect(typeof l.duAn).toBe('boolean')
        expect(l.title.length).toBeGreaterThan(0)
      }
    }
  })

  it('bài chưa viết thì chưa thể có Phần 7', () => {
    for (const c of CHAPTERS) {
      for (const l of c.lessons) {
        if (!l.ready) expect(l.duAn).toBe(false)
      }
    }
  })

  it('LESSON_SECTIONS suy ra đúng từ CHAPTERS, giữ thứ tự chương', () => {
    expect(LESSON_SECTIONS).toHaveLength(30)
    expect(LESSON_SECTIONS[0].sid).toBe('do-phuc-tap')
    expect(LESSON_SECTIONS[0].chapter).toBe('nen-mong')
    expect(LESSON_SECTIONS.at(-1).chapter).toBe('chuyen-de')
  })

  it('sidToFile đổi sid thành đường dẫn section đúng quy ước cũ', () => {
    expect(sidToFile('dfs-bfs')).toBe('src/sections/DfsBfs.vue')
    expect(sidToFile('qhd-lis-lcs-doixung')).toBe('src/sections/QhdLisLcsDoixung.vue')
    expect(sidToFile('do-phuc-tap')).toBe('src/sections/DoPhucTap.vue')
  })

  it('mọi bài ready đều có file section tồn tại thật', () => {
    for (const s of LESSON_SECTIONS.filter(s => s.ready)) {
      expect(existsSync(resolve(__dirname, '..', s.file))).toBe(true)
    }
  })
})
```

Thêm hai import ở đầu file test nếu chưa có:

```js
import { existsSync } from 'node:fs'
import { resolve } from 'node:path'
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/parts.spec.js`
Expected: FAIL — `CHAPTERS` chưa được export.

- [ ] **Step 3: Viết implementation**

Trong `src/lesson/parts.js`, thay khối `LESSON_SECTIONS` viết tay (dòng 25–38) bằng:

```js
// sid 'dfs-bfs' -> 'src/sections/DfsBfs.vue'. Quy ước này đúng với cả 10 file cũ,
// nên không còn phải viết tay đường dẫn ở hai nơi.
export function sidToFile(sid) {
  const pascal = sid.split('-').map(w => w[0].toUpperCase() + w.slice(1)).join('')
  return `src/sections/${pascal}.vue`
}

// Nguồn sự thật duy nhất về thứ tự học. LESSON_SECTIONS và menu trái đều suy ra
// từ đây — đừng định nghĩa thứ tự bài học ở bất kỳ chỗ nào khác.
// ready: đã có file section. duAn: đã có Phần 7 Dự án thực hành.
export const CHAPTERS = [
  {
    key: 'nen-mong', num: 1, title: 'Nền móng', capstoneReady: false,
    lessons: [
      { sid: 'do-phuc-tap', title: 'Độ phức tạp thuật toán và mô hình chi phí', ready: false, duAn: false },
      { sid: 'mang-chuoi', title: 'Mảng, chuỗi và mảng động', ready: false, duAn: false },
      { sid: 'de-quy', title: 'Đệ quy và hệ thức truy hồi', ready: false, duAn: false },
      { sid: 'danh-sach-lien-ket', title: 'Danh sách liên kết', ready: false, duAn: false },
    ],
  },
  {
    key: 'xu-ly-day', num: 2, title: 'Xử lý dãy', capstoneReady: false,
    lessons: [
      { sid: 'sap-xep', title: 'Sắp xếp: merge, quick, counting', ready: false, duAn: false },
      { sid: 'tim-kiem-nhi-phan', title: 'Tìm kiếm nhị phân và tìm nhị phân trên đáp án', ready: false, duAn: false },
      { sid: 'hai-con-tro-cua-so-truot', title: 'Hai con trỏ và cửa sổ trượt', ready: false, duAn: false },
      { sid: 'tong-tien-to', title: 'Tổng tiền tố và mảng hiệu', ready: false, duAn: false },
    ],
  },
  {
    key: 'tra-cuu', num: 3, title: 'Tra cứu', capstoneReady: false,
    lessons: [
      { sid: 'ngan-xep-hang-doi', title: 'Ngăn xếp và hàng đợi', ready: true, duAn: false },
      { sid: 'bang-bam', title: 'Bảng băm', ready: false, duAn: false },
      { sid: 'heap-hang-doi-uu-tien', title: 'Heap và hàng đợi ưu tiên', ready: false, duAn: false },
      { sid: 'trie', title: 'Trie — cây tiền tố', ready: false, duAn: false },
    ],
  },
  {
    key: 'cay', num: 4, title: 'Cây', capstoneReady: false,
    lessons: [
      { sid: 'cay-nhi-phan-bst', title: 'Cây nhị phân và BST', ready: true, duAn: false },
      { sid: 'bst-nang-cao', title: 'BST nâng cao', ready: true, duAn: false },
      { sid: 'fenwick-segment-tree', title: 'Fenwick và Segment Tree', ready: false, duAn: false },
    ],
  },
  {
    key: 'do-thi', num: 5, title: 'Đồ thị', capstoneReady: false,
    lessons: [
      { sid: 'dfs-bfs', title: 'DFS và BFS', ready: true, duAn: false },
      { sid: 'dsu', title: 'DSU — Disjoint Set Union', ready: true, duAn: false },
      { sid: 'sap-xep-to-po', title: 'Sắp xếp tô-pô', ready: false, duAn: false },
      { sid: 'duong-di-ngan-nhat', title: 'Đường đi ngắn nhất có trọng số', ready: false, duAn: false },
      { sid: 'cay-khung-nho-nhat', title: 'Cây khung nhỏ nhất', ready: false, duAn: false },
    ],
  },
  {
    key: 'thiet-ke-thuat-toan', num: 6, title: 'Thiết kế thuật toán', capstoneReady: false,
    lessons: [
      { sid: 'quay-lui-xau-nhi-phan', title: 'Quay lui và xâu nhị phân', ready: true, duAn: false },
      { sid: 'to-hop', title: 'Tổ hợp', ready: true, duAn: false },
      { sid: 'tham-lam', title: 'Tham lam', ready: true, duAn: false },
      { sid: 'qhd-nen-tang', title: 'Quy hoạch động nền tảng', ready: true, duAn: false },
      { sid: 'qhd-lis-lcs-doixung', title: 'QHĐ nâng cao: Knapsack, LIS, LCS, xâu đối xứng', ready: true, duAn: false },
    ],
  },
  {
    key: 'chuyen-de', num: 7, title: 'Chuyên đề và giới hạn', capstoneReady: false,
    lessons: [
      { sid: 'thao-tac-bit', title: 'Thao tác bit', ready: false, duAn: false },
      { sid: 'toan-so-hoc', title: 'Toán và số học: GCD, modulo, sàng, luỹ thừa nhanh', ready: false, duAn: false },
      { sid: 'chuoi-nang-cao', title: 'Chuỗi nâng cao: KMP và hash chuỗi', ready: false, duAn: false },
      { sid: 'work-span', title: 'Work/Span và tư duy song song', ready: false, duAn: false },
      { sid: 'do-kho-bai-toan', title: 'Độ khó bài toán: NP-đầy đủ và khi nào dùng xấp xỉ', ready: false, duAn: false },
    ],
  },
]

export const LESSON_SECTIONS = CHAPTERS.flatMap(c =>
  c.lessons.map(l => ({ ...l, chapter: c.key, file: sidToFile(l.sid) })),
)
```

- [ ] **Step 4: Chạy lại test**

Run: `npm run test -- --run`
Expected: PASS toàn bộ. Nếu `tests/lesson-structure.spec.js` đỏ, kiểm tra rằng nó vẫn lọc `LESSON_SECTIONS.filter(s => lessons[s.sid])` — 20 bài mới chưa có file dữ liệu nên tự động bị loại.

- [ ] **Step 5: Commit**

```bash
git add src/lesson/parts.js tests/parts.spec.js
git commit -m "feat(lesson): CHAPTERS 7 chuong 30 nhom, suy ra LESSON_SECTIONS"
```

---

## Task 2: `nav.js` suy ra từ `CHAPTERS`

**Files:**
- Modify: `src/data/nav.js` (thay toàn bộ)
- Create: `tests/nav.spec.js`

**Interfaces:**
- Consumes: `CHAPTERS`, `LESSON_SECTIONS` từ Task 1.
- Produces: `navGroups` — `[{ label, items: [{ id, label, ready }] }]`, một nhóm mỗi chương.
- Produces: `navTop`, `allSectionIds`, `DEFAULT_ID` giữ nguyên tên như cũ.
- Produces: `FIRST_LESSON_ID` — sid của bài `ready` đầu tiên, dùng cho nút "Bắt đầu học".

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/nav.spec.js`:

```js
import { describe, it, expect } from 'vitest'
import { navTop, navGroups, allSectionIds, DEFAULT_ID, FIRST_LESSON_ID } from '../src/data/nav.js'
import { CHAPTERS } from '../src/lesson/parts.js'

describe('nav.js suy ra từ CHAPTERS', () => {
  it('có đúng 7 nhóm, nhãn mang số chương và tên chương', () => {
    expect(navGroups).toHaveLength(7)
    expect(navGroups[0].label).toBe('Chương 1 — Nền móng')
    expect(navGroups[6].label).toBe('Chương 7 — Chuyên đề và giới hạn')
  })

  it('thứ tự và số lượng bài khớp CHAPTERS', () => {
    const fromNav = navGroups.flatMap(g => g.items.map(i => i.id))
    const fromChapters = CHAPTERS.flatMap(c => c.lessons.map(l => l.sid))
    expect(fromNav).toEqual(fromChapters)
  })

  it('mỗi mục mang theo cờ ready để sidebar biết bài nào chưa viết', () => {
    const items = navGroups.flatMap(g => g.items)
    expect(items.every(i => typeof i.ready === 'boolean')).toBe(true)
  })

  it('allSectionIds chỉ chứa trang chủ và các bài đã viết', () => {
    expect(allSectionIds).toContain('trang-chu')
    expect(allSectionIds).toContain('dsu')
    expect(allSectionIds).not.toContain('bang-bam')
  })

  it('FIRST_LESSON_ID là bài ready đầu tiên theo thứ tự học', () => {
    expect(FIRST_LESSON_ID).toBe('ngan-xep-hang-doi')
  })

  it('DEFAULT_ID vẫn là trang chủ', () => {
    expect(DEFAULT_ID).toBe('trang-chu')
  })
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/nav.spec.js`
Expected: FAIL — `FIRST_LESSON_ID` chưa tồn tại, `navGroups` còn nhãn "Nhóm 1 — Vét cạn".

- [ ] **Step 3: Viết implementation**

Thay toàn bộ `src/data/nav.js`:

```js
// Menu trái KHÔNG được viết tay. Nó suy ra từ CHAPTERS trong src/lesson/parts.js,
// nếu không sẽ có hai nơi cùng định nghĩa thứ tự bài học và chúng sẽ lệch nhau.
import { CHAPTERS, LESSON_SECTIONS } from '../lesson/parts.js'

export const navTop = [
  { id: 'trang-chu', label: '📘 Trang chủ' },
]

export const navGroups = CHAPTERS.map(c => ({
  label: `Chương ${c.num} — ${c.title}`,
  items: c.lessons.map(l => ({ id: l.sid, label: l.title, ready: l.ready })),
}))

// Chỉ bài đã viết mới điều hướng được. Bài chưa viết vẫn hiện trên sidebar để
// người học thấy lộ trình, nhưng không phải là section thật.
export const allSectionIds = [
  ...navTop.map(i => i.id),
  ...LESSON_SECTIONS.filter(l => l.ready).map(l => l.sid),
]

export const FIRST_LESSON_ID = LESSON_SECTIONS.find(l => l.ready).sid

export const DEFAULT_ID = 'trang-chu'
```

- [ ] **Step 4: Chạy lại test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: test PASS, build không cảnh báo.

- [ ] **Step 5: Commit**

```bash
git add src/data/nav.js tests/nav.spec.js
git commit -m "refactor(nav): sinh menu trai tu CHAPTERS thay vi viet tay"
```

---

## Task 3: Sidebar hiển thị bài chưa viết dạng mờ

**Files:**
- Modify: `src/App.vue:20-50` (khối `.sb-list`), `src/App.vue:24` (nút Bắt đầu học), `src/App.vue:119` (import)
- Modify: `src/style.css` (thêm sau khối `.sidebar .sb-group`, khoảng dòng 102)
- Modify: `tests/app-shell.spec.js`

**Interfaces:**
- Consumes: `navGroups[].items[].ready`, `FIRST_LESSON_ID` từ Task 2.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `tests/app-shell.spec.js`:

```js
import { readFileSync } from 'node:fs'
import { resolve } from 'node:path'

describe('sidebar với bài chưa viết', () => {
  const src = readFileSync(resolve(__dirname, '..', 'src/App.vue'), 'utf8')

  it('bài chưa viết render thành <span>, không phải <a> bấm được', () => {
    expect(src).toContain('v-if="item.ready"')
    expect(src).toContain('class="sb-soon"')
  })

  it('nút Bắt đầu học trỏ tới bài ready đầu tiên, không hardcode sid', () => {
    expect(src).toContain('goToId(FIRST_LESSON_ID)')
    expect(src).not.toContain("goToId('quay-lui-xau-nhi-phan')")
  })

  it('style.css có lớp sb-soon và giữ chiều cao chạm tối thiểu', () => {
    const css = readFileSync(resolve(__dirname, '..', 'src/style.css'), 'utf8')
    expect(css).toMatch(/\.sidebar \.sb-soon\s*\{/)
    expect(css).toMatch(/min-height:\s*44px/)
  })
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/app-shell.spec.js`
Expected: FAIL — chưa có `sb-soon`.

- [ ] **Step 3: Sửa `src/App.vue`**

Đổi nút bắt đầu (dòng 21–26) thành:

```html
        <a class="sb-top sb-start" href="#" @click.prevent="goToId(FIRST_LESSON_ID)"
          >▶ Bắt đầu học</a
        >
```

Đổi khối lặp nhóm (dòng 38–49) thành:

```html
        <template v-for="group in navGroups" :key="group.label">
          <div class="sb-group">{{ group.label }}</div>
          <template v-for="item in group.items" :key="item.id">
            <a
              v-if="item.ready"
              href="#"
              :class="{ active: activeSection === item.id }"
              :aria-current="activeSection === item.id ? 'page' : undefined"
              @click.prevent="goToId(item.id)"
              >{{ item.label }}</a
            >
            <!-- Bài chưa viết vẫn hiện để người học thấy lộ trình, nhưng không
                 phải là link — bấm vào sẽ không đi đâu cả. -->
            <span v-else class="sb-soon">{{ item.label }}<em class="sb-soon-tag">sắp có</em></span>
          </template>
        </template>
```

Sửa import ở dòng 119:

```js
import { navTop, navGroups, allSectionIds, DEFAULT_ID, FIRST_LESSON_ID } from "./data/nav.js";
```

- [ ] **Step 4: Thêm CSS vào `src/style.css`**

Chèn ngay sau `.sidebar .sb-group:first-child { margin-top: 0; }`:

```css
  /* Bài chưa viết: đủ tương phản để đọc được (AA 4.5:1 trên nền sidebar),
     nhưng khác hẳn link thật để không ai bấm nhầm. */
  .sidebar .sb-soon {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem;
    min-height: 44px;
    padding: 0.5rem 0.75rem;
    color: var(--muted);
    cursor: default;
  }
  .sidebar .sb-soon-tag {
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.1rem 0.4rem;
    border-radius: 999px;
    border: 1px solid var(--line);
    color: var(--muted);
    white-space: nowrap;
  }
```

- [ ] **Step 5: Chạy lại test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS, build sạch.

- [ ] **Step 6: Kiểm chứng bằng mắt**

Run: `npm run dev`, mở trình duyệt.
Kiểm: sidebar hiện 7 tiêu đề chương; 20 bài chưa viết hiện mờ kèm nhãn "sắp có" và không bấm được; ở màn hẹp dưới 900px sidebar vẫn gấp/mở đúng; nhãn "sắp có" đọc được rõ.
Nếu không mở được trình duyệt, ghi rõ vào commit là chưa kiểm chứng bằng mắt.

- [ ] **Step 7: Commit**

```bash
git add src/App.vue src/style.css tests/app-shell.spec.js
git commit -m "feat(nav): sidebar theo chuong, bai chua viet hien mo"
```

---

## Task 4: Thêm Phần 7 vào khung bài học

**Files:**
- Modify: `src/lesson/parts.js:3-11`
- Modify: `tests/parts.spec.js`, `tests/menu.spec.js`

**Interfaces:**
- Produces: `LESSON_PARTS` có thêm `{ key: 'du-an', num: 7, title: 'Dự án thực hành' }`.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `tests/parts.spec.js`:

```js
it('khung bài học có 8 mục, kết thúc bằng Dự án thực hành', () => {
  expect(LESSON_PARTS).toHaveLength(8)
  const last = LESSON_PARTS.at(-1)
  expect(last.key).toBe('du-an')
  expect(last.num).toBe(7)
  expect(partTitle('du-an')).toBe('7. Dự án thực hành')
})
```

Thêm vào `tests/menu.spec.js`:

```js
it('menu phải tự sinh thêm mục Dự án thực hành', () => {
  const menu = buildMenu('dsu')
  expect(menu.map(m => m.id)).toContain('dsu--du-an')
  expect(menu.at(-1).label).toBe('7. Dự án thực hành')
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/parts.spec.js tests/menu.spec.js`
Expected: FAIL — `LESSON_PARTS` mới có 7 phần tử.

- [ ] **Step 3: Viết implementation**

Thêm dòng cuối vào mảng `LESSON_PARTS` trong `src/lesson/parts.js`, ngay sau dòng `leetcode`:

```js
  { key: 'du-an', num: 7, title: 'Dự án thực hành' },
```

- [ ] **Step 4: Chạy lại test**

Run: `npm run test -- --run`
Expected: `tests/lesson-structure.spec.js` **FAIL 10 bài** ở test "dùng đủ 7 mục chuẩn", vì 10 bài cũ chưa có `part="du-an"`.

- [ ] **Step 5: Sửa test cấu trúc để chỉ ép Phần 7 với bài đã có Phần 7**

Trong `tests/lesson-structure.spec.js`, đổi test ở dòng 20–29 thành:

```js
  it('dùng đủ các mục chuẩn của khung bài học', () => {
    const sec = LESSON_SECTIONS.find(s => s.sid === sid)
    for (const p of LESSON_PARTS) {
      // Phần 7 chỉ bắt buộc với bài đã được bổ sung Dự án thực hành.
      // Giai đoạn 4 sẽ bật cờ duAn cho cả 10 bài cũ, lúc đó luật này phủ toàn bộ.
      if (p.key === 'du-an' && !sec.duAn) continue
      if (p.key === 'muc-tieu') {
        expect(src).toContain('<LessonGoal')
      } else {
        expect(src).toContain(`part="${p.key}"`)
      }
    }
    expect(src).toContain(`:sid="'${sid}'"`)
  })
```

- [ ] **Step 6: Chạy lại test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS toàn bộ.

- [ ] **Step 7: Commit**

```bash
git add src/lesson/parts.js tests/parts.spec.js tests/menu.spec.js tests/lesson-structure.spec.js
git commit -m "feat(lesson): them Phan 7 Du an thuc hanh vao khung bai hoc"
```

---

## Task 5: Component `ProjectBrief.vue`

**Files:**
- Create: `src/components/ProjectBrief.vue`
- Create: `tests/components/project-brief.spec.js`
- Modify: `src/style.css` (thêm vào cuối file)

**Interfaces:**
- Consumes: không có.
- Produces: component nhận props `brief` (Object, bắt buộc) và `mode` (String, `'project'` hoặc `'capstone'`, mặc định `'project'`).
- `brief` cho `mode="project"`: `{ title, why, input, must: [], done: [], traps: [] }`.
- `brief` cho `mode="capstone"` có thêm: `{ uses: [], reuses: [{ chapter, module }], stretch: [], data: { format, sample, url } }`.

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/components/project-brief.spec.js`:

```js
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import ProjectBrief from '../../src/components/ProjectBrief.vue'

const projectBrief = {
  title: 'Bộ đếm tần suất từ',
  why: 'Bước đầu của mọi công cụ phân tích văn bản.',
  input: 'File .txt bất kỳ',
  must: ['Đọc file, tách từ, đếm', 'Tự cài hash table'],
  done: ['Chạy file 1MB dưới 1 giây'],
  traps: ['Quên xử lý va chạm'],
}

const capstoneBrief = {
  ...projectBrief,
  title: 'bench — công cụ đo hiệu năng',
  uses: ['do-phuc-tap', 'mang-chuoi'],
  reuses: [],
  stretch: ['Xuất kết quả ra CSV'],
  data: { format: 'CSV hai cột', sample: 'n,ms\n1000,12', url: 'https://example.org/data' },
}

describe('ProjectBrief', () => {
  it('chế độ project hiện đủ 5 khối nội dung', () => {
    const w = mount(ProjectBrief, { props: { brief: projectBrief } })
    const text = w.text()
    expect(text).toContain('Bộ đếm tần suất từ')
    expect(text).toContain('Bước đầu của mọi công cụ')
    expect(text).toContain('File .txt bất kỳ')
    expect(text).toContain('Tự cài hash table')
    expect(text).toContain('Chạy file 1MB dưới 1 giây')
    expect(text).toContain('Quên xử lý va chạm')
  })

  it('chế độ project không hiện khối riêng của capstone', () => {
    const w = mount(ProjectBrief, { props: { brief: projectBrief } })
    expect(w.find('.pb-capstone').exists()).toBe(false)
    expect(w.text()).not.toContain('Dữ liệu mẫu')
  })

  it('chế độ capstone hiện nhãn MVP, dữ liệu mẫu và link tải', () => {
    const w = mount(ProjectBrief, { props: { brief: capstoneBrief, mode: 'capstone' } })
    expect(w.find('.pb-capstone').exists()).toBe(true)
    expect(w.text()).toContain('Dự án MVP cuối chương')
    expect(w.text()).toContain('Dữ liệu mẫu')
    expect(w.find('a.pb-data-link').attributes('href')).toBe('https://example.org/data')
  })

  it('liệt kê module bắt buộc dùng lại khi có', () => {
    const brief = { ...capstoneBrief, reuses: [{ chapter: 2, module: 'sort' }] }
    const w = mount(ProjectBrief, { props: { brief, mode: 'capstone' } })
    expect(w.text()).toContain('Chương 2')
    expect(w.text()).toContain('sort')
  })

  it('không dùng thẻ i, đúng luật không chữ nghiêng', () => {
    const w = mount(ProjectBrief, { props: { brief: capstoneBrief, mode: 'capstone' } })
    expect(w.html()).not.toContain('<i>')
  })
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/components/project-brief.spec.js`
Expected: FAIL — file component chưa tồn tại.

- [ ] **Step 3: Viết `src/components/ProjectBrief.vue`**

```vue
<template>
  <div class="pb" :class="{ 'pb-capstone': isCapstone }">
    <p v-if="isCapstone" class="pb-badge">Dự án MVP cuối chương</p>
    <h4 class="pb-title">{{ brief.title }}</h4>

    <p class="pb-why">{{ brief.why }}</p>

    <div class="pb-block">
      <strong>Đầu vào:</strong> {{ brief.input }}
    </div>

    <div class="pb-block">
      <strong>Yêu cầu bắt buộc</strong>
      <ul>
        <li v-for="(m, i) in brief.must" :key="i">{{ m }}</li>
      </ul>
    </div>

    <div v-if="isCapstone && brief.reuses.length" class="pb-block pb-reuse">
      <strong>Bắt buộc dùng lại code cũ của bạn</strong>
      <ul>
        <li v-for="(r, i) in brief.reuses" :key="i">
          Chương {{ r.chapter }} — module <code>{{ r.module }}</code>
        </li>
      </ul>
    </div>

    <div class="pb-block">
      <strong>Coi như xong khi</strong>
      <ul>
        <li v-for="(d, i) in brief.done" :key="i">{{ d }}</li>
      </ul>
    </div>

    <div v-if="isCapstone" class="pb-block">
      <strong>Dữ liệu mẫu</strong>
      <p>{{ brief.data.format }}</p>
      <!-- Dữ liệu mẫu đến từ biến nên phải nội suy — KHÔNG dùng v-pre ở đây,
           khác với code C++ viết tay trong section. -->
      <pre class="pb-sample"><code>{{ brief.data.sample }}</code></pre>
      <a class="pb-data-link" :href="brief.data.url" target="_blank" rel="noopener">
        Tải bộ dữ liệu đầy đủ
      </a>
    </div>

    <details class="pb-block">
      <summary>Chỗ dễ sai</summary>
      <ul>
        <li v-for="(t, i) in brief.traps" :key="i">{{ t }}</li>
      </ul>
    </details>

    <details v-if="isCapstone && brief.stretch.length" class="pb-block">
      <summary>Mở rộng nếu còn hứng</summary>
      <ul>
        <li v-for="(s, i) in brief.stretch" :key="i">{{ s }}</li>
      </ul>
    </details>
  </div>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  brief: { type: Object, required: true },
  mode: { type: String, default: 'project' },
})

const isCapstone = computed(() => props.mode === 'capstone')
</script>
```

- [ ] **Step 4: Thêm CSS vào cuối `src/style.css`**

```css
/* Dự án thực hành. Hộp có nền nên KHÔNG bó theo --measure — nó dùng trọn
   bề ngang cột nội dung, giống các hộp khác của bài học. */
.pb {
  border: 1px solid var(--line);
  border-radius: 12px;
  padding: 1rem 1.1rem;
  background: var(--card);
}
.pb-capstone {
  border-width: 2px;
  border-color: var(--accent);
}
.pb-badge {
  margin: 0 0 0.4rem;
  font-size: 0.8rem;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  color: var(--accent-ink);
}
.pb-title { margin: 0 0 0.5rem; font-size: 1.05rem; }
.pb-why { margin: 0 0 0.8rem; }
.pb-block { margin-top: 0.9rem; }
.pb-block ul { margin: 0.35rem 0 0; padding-left: 1.2rem; }
.pb-block li { margin: 0.25rem 0; }
.pb-reuse { border-left: 3px solid var(--accent); padding-left: 0.75rem; }
.pb-sample { overflow-x: auto; }
.pb-data-link { display: inline-block; margin-top: 0.5rem; min-height: 44px; line-height: 44px; }
```

Trước khi dán, chạy `grep -n "^\s*\.pb\b\|^\s*\.pb-" src/style.css` để chắc chắn chưa có selector nào trùng. Kiểm tra `--card`, `--accent`, `--accent-ink` có thật trong `:root`; nếu tên biến khác thì dùng đúng tên đang có.

- [ ] **Step 5: Chạy lại test**

Run: `npm run test -- --run tests/components/project-brief.spec.js`
Expected: PASS 5/5.

- [ ] **Step 6: Commit**

```bash
git add src/components/ProjectBrief.vue src/style.css tests/components/project-brief.spec.js
git commit -m "feat(component): ProjectBrief hai che do project va capstone"
```

---

## Task 6: Schema `project` và cờ `duAn`

**Files:**
- Modify: `tests/lesson-data.spec.js`

**Interfaces:**
- Produces: luật — mọi bài có `duAn: true` trong `CHAPTERS` phải có `project` đủ 6 trường trong file dữ liệu.

- [ ] **Step 1: Viết test thất bại**

Thêm vào `tests/lesson-data.spec.js`:

```js
import { LESSON_SECTIONS } from '../src/lesson/parts.js'

const coDuAn = LESSON_SECTIONS.filter(s => s.duAn)

describe('schema project', () => {
  it('luật này sẽ phủ dần khi các bài bật cờ duAn', () => {
    expect(Array.isArray(coDuAn)).toBe(true)
  })

  describe.each(coDuAn)('bài $sid', ({ sid }) => {
    const p = lessons[sid].project

    it('có project đủ 6 trường', () => {
      expect(p).toBeTypeOf('object')
      for (const k of ['title', 'why', 'input']) {
        expect(typeof p[k]).toBe('string')
        expect(p[k].length).toBeGreaterThan(10)
      }
      for (const k of ['must', 'done', 'traps']) {
        expect(Array.isArray(p[k])).toBe(true)
        expect(p[k].length).toBeGreaterThan(0)
      }
    })

    it('có tối thiểu 2 yêu cầu bắt buộc và 1 tiêu chí xong', () => {
      expect(p.must.length).toBeGreaterThanOrEqual(2)
      expect(p.done.length).toBeGreaterThanOrEqual(1)
    })

    it('yêu cầu cuối cùng bắc cầu về kiến thức cũ', () => {
      expect(p.must.at(-1).length).toBeGreaterThan(20)
    })

    it('section render Phần 7 bằng ProjectBrief', () => {
      const src = readFileSync(resolve(root, sidToFile(sid)), 'utf8')
      expect(src).toContain('part="du-an"')
      expect(src).toContain('<ProjectBrief')
    })
  })
})
```

Thêm import `sidToFile` và `readFileSync`/`resolve`/`root` ở đầu file nếu chưa có.

- [ ] **Step 2: Chạy test**

Run: `npm run test -- --run tests/lesson-data.spec.js`
Expected: PASS, nhưng chỉ có test đầu tiên chạy vì `coDuAn` đang rỗng. Đây là test dẫn đường: nó sẽ tự bật khi Task 10 và 11 đặt `duAn: true`.

- [ ] **Step 3: Ghi nhận đây là test dẫn đường**

Không sửa test cho có việc. Nó rỗng là đúng thiết kế — Task 10 là bài đầu tiên làm nó chạy thật.

- [ ] **Step 4: Commit**

```bash
git add tests/lesson-data.spec.js
git commit -m "test(lesson): ep schema project cho bai da bat co duAn"
```

---

## Task 7: Cờ `recall` cho câu quiz ôn tập

**Files:**
- Modify: `src/components/QuizBlock.vue:8-10`
- Modify: `tests/lesson-data.spec.js`
- Modify: `tests/components/` — file test QuizBlock hiện có
- Modify: `src/style.css` (cuối file)

**Interfaces:**
- Consumes: `questions[].recall` (Boolean, tuỳ chọn).
- Produces: câu quiz có `recall: true` hiện thêm nhãn "Ôn lại bài trước".

- [ ] **Step 1: Viết test thất bại**

Thêm vào file test QuizBlock trong `tests/components/`:

```js
it('câu ôn tập có nhãn riêng, câu thường thì không', () => {
  const w = mount(QuizBlock, {
    props: {
      questions: [
        { q: 'Câu ôn', options: ['a', 'b'], answer: 0, why: 'vì thế', recall: true },
        { q: 'Câu thường', options: ['a', 'b'], answer: 1, why: 'vì vậy' },
      ],
    },
  })
  expect(w.findAll('.quiz-recall')).toHaveLength(1)
  expect(w.text()).toContain('Ôn lại bài trước')
})
```

Thêm vào `tests/lesson-data.spec.js`, trong khối `describe.each(coDuAn)`:

```js
    it('có tối thiểu 1 câu quiz ôn lại kiến thức cũ', () => {
      const recalls = lessons[sid].quiz.filter(q => q.recall === true)
      expect(recalls.length).toBeGreaterThanOrEqual(1)
    })
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/components/`
Expected: FAIL — không tìm thấy `.quiz-recall`.

- [ ] **Step 3: Sửa `src/components/QuizBlock.vue`**

Đổi khối `quiz-prompt` (dòng 8–10) thành:

```html
      <p class="quiz-prompt">
        <strong>Câu {{ qi + 1 }}.</strong>
        <span v-if="item.recall" class="quiz-recall">Ôn lại bài trước</span>
        {{ item.q }}
      </p>
```

- [ ] **Step 4: Thêm CSS vào cuối `src/style.css`**

```css
/* Nhãn câu ôn tập. Dùng màu chữ đậm trên nền nhạt để đạt AA 4.5:1 —
   không lấy màu viền làm màu chữ, đó là lỗi cũ của nhãn Easy/Medium/Hard. */
.quiz-recall {
  display: inline-block;
  margin-right: 0.4rem;
  padding: 0.1rem 0.45rem;
  border-radius: 999px;
  font-size: 0.75rem;
  font-weight: 700;
  background: var(--chip-bg);
  color: var(--chip-ink);
}
```

Chạy `grep -n "chip-bg\|chip-ink" src/style.css` trước. Nếu hai biến này chưa có, dùng cặp biến nền/chữ đã đạt AA đang dùng cho nhãn khác trong file, đừng tự đặt màu mới.

- [ ] **Step 5: Chạy lại test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/components/QuizBlock.vue src/style.css tests/components tests/lesson-data.spec.js
git commit -m "feat(quiz): nhan cau on tap chong quen kien thuc cu"
```

---

## Task 8: Đặc tả đầy đủ 7 MVP

**Files:**
- Create: `docs/superpowers/specs/2026-08-09-dac-ta-7-mvp.md`

Đây là task tài liệu, không có code, nhưng là task quan trọng nhất của Giai đoạn 1: nó khoá ràng buộc kế thừa chéo chương trước khi bất kỳ bài nào được viết.

- [ ] **Step 1: Viết đặc tả**

Mỗi MVP trong bảy MVP phải có đủ các mục sau, không được để trống mục nào:

1. Tên và một câu mô tả sản phẩm.
2. Bối cảnh thật: ai dùng, giải quyết vấn đề gì, phần mềm thật nào đang làm việc này.
3. Định dạng dữ liệu đầu vào, 20 dòng mẫu dán trực tiếp, và link tải bộ dữ liệu công khai.
4. Danh sách yêu cầu bắt buộc, mỗi yêu cầu là một câu kiểm được.
5. Tiêu chí "coi như xong": test nào phải xanh, con số hiệu năng nào phải đạt.
6. Bảng `reuses`: module nào của chương nào bắt buộc phải liên kết vào, và ở đúng chỗ nào trong chương trình.
7. Gợi ý chia module, mỗi module một trách nhiệm.
8. Chỗ dễ sai.
9. Mở rộng nếu còn hứng.

Nội dung bảy MVP lấy từ bảng ở mục 6.2 của spec:

| Ch | MVP | reuses |
|---|---|---|
| 1 `nen-mong` | `core` + `bench` | rỗng (chương gốc) |
| 2 `xu-ly-day` | Bộ phân tích log máy chủ | `core`, `bench` của Ch1 |
| 3 `tra-cuu` | Máy gợi ý tìm kiếm | `core` Ch1, `sort` Ch2, `bench` Ch1 |
| 4 `cay` | Mini database có index | truy vấn log Ch2, `bench` Ch1 |
| 5 `do-thi` | Trình lập lộ trình giao hàng | `heap` Ch3, `hash` Ch3, `core` Ch1 |
| 6 `thiet-ke-thuat-toan` | Xếp lịch và tối ưu ngân sách | `sort` Ch2, `heap` Ch3 |
| 7 `chuyen-de` | Phát hiện tài liệu trùng lặp | `trie` Ch3, `hash` Ch3, kho văn bản Ch2 |

- [ ] **Step 2: Tự rà**

Kiểm ba điều, sửa ngay tại chỗ nếu sai:
- Mọi module xuất hiện trong cột `reuses` phải là thứ người học thật sự viết ra ở chương được nhắc. Nếu không có bài nào bảo họ viết `sort` thành module tái dùng được, sửa đặc tả Ch2 để có.
- Mọi link tải dữ liệu phải là nguồn công khai, không đăng nhập.
- Không mục nào để trống hoặc ghi "sẽ bổ sung".

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/specs/2026-08-09-dac-ta-7-mvp.md
git commit -m "docs: dac ta day du 7 MVP va rang buoc ke thua cheo chuong"
```

---

## Task 9: Module capstone và MVP Chương 1

**Files:**
- Create: `src/data/capstones/nen-mong.js`
- Create: `src/data/capstones/index.js`
- Create: `tests/capstone.spec.js`
- Modify: `src/lesson/parts.js` (đặt `capstoneReady: true` cho chương `nen-mong`)

**Interfaces:**
- Consumes: đặc tả từ Task 8; schema `capstone` từ Task 5.
- Produces: `capstones` — object khoá theo `chapter.key`, giá trị là brief đúng schema `capstone`.
- Produces: `capstoneCuaChuong(chapterKey)` — trả brief hoặc `null`.

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/capstone.spec.js`:

```js
import { describe, it, expect } from 'vitest'
import { CHAPTERS } from '../src/lesson/parts.js'
import { capstones, capstoneCuaChuong } from '../src/data/capstones/index.js'

const san = CHAPTERS.filter(c => c.capstoneReady)

describe('capstone', () => {
  it('chỉ chương đã bật cờ mới có dữ liệu MVP', () => {
    for (const c of CHAPTERS) {
      expect(!!capstones[c.key]).toBe(c.capstoneReady)
    }
  })

  describe.each(san)('chương $key', ({ key, num }) => {
    const cap = capstoneCuaChuong(key)

    it('đủ trường của schema capstone', () => {
      for (const k of ['title', 'why', 'input']) {
        expect(typeof cap[k]).toBe('string')
        expect(cap[k].length).toBeGreaterThan(10)
      }
      for (const k of ['must', 'done', 'traps', 'uses', 'reuses', 'stretch']) {
        expect(Array.isArray(cap[k])).toBe(true)
      }
      expect(typeof cap.data.format).toBe('string')
      expect(typeof cap.data.sample).toBe('string')
      expect(cap.data.url).toMatch(/^https:\/\//)
    })

    it('uses chỉ trỏ tới sid có thật', () => {
      const moiSid = CHAPTERS.flatMap(c => c.lessons.map(l => l.sid))
      for (const u of cap.uses) expect(moiSid).toContain(u)
    })

    it('reuses chỉ trỏ tới chương có số nhỏ hơn', () => {
      for (const r of cap.reuses) expect(r.chapter).toBeLessThan(num)
    })

    it('chương từ 2 trở đi bắt buộc kế thừa tối thiểu 2 module', () => {
      if (num === 1) expect(cap.reuses).toHaveLength(0)
      else expect(cap.reuses.length).toBeGreaterThanOrEqual(2)
    })
  })
})
```

- [ ] **Step 2: Chạy test để chắc chắn nó fail**

Run: `npm run test -- --run tests/capstone.spec.js`
Expected: FAIL — chưa có `src/data/capstones/index.js`.

- [ ] **Step 3: Viết `src/data/capstones/nen-mong.js`**

Nội dung lấy từ đặc tả MVP Chương 1 ở Task 8. Khung dữ liệu:

```js
export default {
  title: 'core + bench — thư viện nền và công cụ đo hiệu năng',
  why: '...bối cảnh thật, ai dùng, phần mềm thật nào đang làm việc này...',
  input: '...định dạng đầu vào...',
  must: ['...', '...'],
  done: ['...', '...'],
  traps: ['...'],
  uses: ['do-phuc-tap', 'mang-chuoi', 'de-quy', 'danh-sach-lien-ket'],
  reuses: [],
  stretch: ['...'],
  data: { format: '...', sample: '...', url: 'https://...' },
}
```

- [ ] **Step 4: Viết `src/data/capstones/index.js`**

```js
import nenMong from './nen-mong.js'

// Khoá đúng bằng CHAPTERS[].key. Chương chưa có MVP thì vắng mặt ở đây và
// cờ capstoneReady của nó phải là false — test capstone.spec.js ép đúng luật này.
export const capstones = {
  'nen-mong': nenMong,
}

export function capstoneCuaChuong(chapterKey) {
  return capstones[chapterKey] || null
}
```

- [ ] **Step 5: Bật cờ trong `parts.js`**

Đổi chương `nen-mong` thành `capstoneReady: true`.

- [ ] **Step 6: Chạy lại test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/data/capstones src/lesson/parts.js tests/capstone.spec.js
git commit -m "feat(capstone): module MVP theo chuong, MVP Chuong 1 core + bench"
```

---

## Task 10: Bài mẫu 1 — `do-phuc-tap`

**Files:**
- Create: `src/data/lessons/do-phuc-tap.js`
- Create: `src/sections/DoPhucTap.vue`
- Modify: `src/data/lessons/index.js`
- Modify: `src/App.vue` (import và render section mới)
- Modify: `src/lesson/parts.js` (`ready: true`, `duAn: true` cho `do-phuc-tap`)

**Interfaces:**
- Consumes: `ProjectBrief.vue` (Task 5), cờ `recall` (Task 7), `LESSON_PARTS` 8 mục (Task 4).
- Produces: bài mẫu định hình khuôn cho 18 bài Giai đoạn 3.

Đây là bài dị biệt nhất trong 30 bài: nó không dạy thuật toán nào, chỉ dạy cách đo. Vì vậy nó được viết bằng Opus và trở thành khuôn cho các bài lý thuyết khác.

**Yêu cầu nội dung:**
- Mục tiêu: 3 gạch đầu dòng, mỗi cái là một việc người học làm được sau bài.
- Lý thuyết: mở bằng ví dụ đời thường trước thuật ngữ, theo đúng lối Feynman. Phủ: đếm phép toán thay vì đếm giây, O lớn là gì, bảng so sánh O(1)/O(log n)/O(n)/O(n log n)/O(n²)/O(2ⁿ) kèm con số thật với n = 10, 1000, 1 triệu, và mô hình bộ nhớ với cache (vì sao duyệt mảng nhanh hơn duyệt danh sách liên kết dù cùng O(n)).
- Vì sao quan trọng: gắn với việc thật, không nói chuyện thi cử.
- Quiz: 4 câu, mỗi câu có `why` giải thích đủ dài. Bài này là bài đầu chương trình nên chưa có kiến thức cũ để ôn — đặt câu `recall: true` hỏi lại chính khái niệm vừa học ở phần đầu bài, và ghi rõ trong `why` rằng từ bài sau câu này sẽ hỏi bài trước.
- Ví dụ điển hình: 2 ví dụ có `id` neo, code C++ trong `<pre v-pre><code>`.
- Bài tập kiểm tra: 3 bài, mỗi bài có `idea` và `hint`.
- Tài nguyên LeetCode: bài này không có bài LeetCode tương ứng trực tiếp; liệt kê 3–5 bài dễ để tự đo thời gian chạy hai cách giải khác nhau, ghi rõ trong `note` rằng mục đích là so hai lời giải chứ không phải giải cho xong.
- Dự án thực hành: `project` đủ 6 trường, yêu cầu cuối cùng bắc cầu tới bài kế tiếp.

- [ ] **Step 1: Viết file dữ liệu**

Tạo `src/data/lessons/do-phuc-tap.js` theo đúng khuôn của `src/data/lessons/dsu.js`, cộng thêm khối `project`:

```js
export default {
  goal: [...],
  examples: [
    { id: 'vd-dpt-dem-phep-toan', title: '...', official: false },
    { id: 'vd-dpt-so-sanh-hai-cach', title: '...', official: false },
  ],
  quiz: [...],       // 4 câu, tối thiểu 1 câu recall: true
  practice: [...],   // 3 bài, mỗi bài có title/idea/hint
  leetcode: [...],
  project: {
    title: '...', why: '...', input: '...',
    must: [...], done: [...], traps: [...],
  },
}
```

- [ ] **Step 2: Đăng ký vào `src/data/lessons/index.js`**

Thêm import và khoá `'do-phuc-tap'` vào object `lessons`, giữ đúng thứ tự alphabet như các dòng có sẵn.

- [ ] **Step 3: Viết `src/sections/DoPhucTap.vue`**

Sao khuôn từ `src/sections/Dsu.vue`, giữ nguyên hợp đồng với App.vue, và thêm Phần 7 ở cuối:

```html
    <LessonPart :sid="'do-phuc-tap'" part="du-an">
      <ProjectBrief :brief="data.project" />
    </LessonPart>
```

Nhớ import `ProjectBrief` trong khối `<script setup>`.

- [ ] **Step 4: Nối vào `src/App.vue`**

Thêm `import DoPhucTap from "./sections/DoPhucTap.vue";` và thẻ `<DoPhucTap :active="activeSection === 'do-phuc-tap'" />` vào `.content-main`, đặt trước `<QuayLuiXauNhiPhan ...>` để thứ tự DOM khớp thứ tự học.

- [ ] **Step 5: Bật cờ trong `parts.js`**

Đổi `do-phuc-tap` thành `ready: true, duAn: true`.

- [ ] **Step 6: Sửa kỳ vọng `FIRST_LESSON_ID` trong `tests/nav.spec.js`**

Bài `ready` đầu tiên vừa đổi từ `ngan-xep-hang-doi` sang `do-phuc-tap`. Sửa dòng kỳ vọng:

```js
    expect(FIRST_LESSON_ID).toBe('do-phuc-tap')
```

Đây là thay đổi đúng, không phải sửa test cho xanh: nút "Bắt đầu học" bây giờ phải đưa người mới tới bài đầu chương trình thật.

- [ ] **Step 7: Chạy test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS. Các test schema `project` và `recall` từ Task 6, 7 lần đầu tiên chạy thật ở đây.

- [ ] **Step 8: Kiểm chứng bằng mắt**

Run: `npm run dev`. Kiểm: bài mới nằm đầu sidebar dưới "Chương 1 — Nền móng"; menu phải hiện đủ 8 mục kể cả "7. Dự án thực hành"; hộp dự án hiển thị đúng; nhãn "Ôn lại bài trước" đọc rõ; không có chữ nghiêng.

- [ ] **Step 9: Commit**

```bash
git add src/data/lessons src/sections/DoPhucTap.vue src/App.vue src/lesson/parts.js
git commit -m "content: bai Do phuc tap thuat toan, bai mau khung 8 muc"
```

---

## Task 11: Bài mẫu 2 — `bang-bam`

**Files:**
- Create: `src/data/lessons/bang-bam.js`
- Create: `src/sections/BangBam.vue`
- Modify: `src/data/lessons/index.js`, `src/App.vue`, `src/lesson/parts.js`

**Interfaces:**
- Consumes: giống Task 10.
- Produces: bài mẫu cho dạng "một cấu trúc dữ liệu cụ thể" — dạng chiếm đa số trong 18 bài còn lại.

**Yêu cầu nội dung:**
- Giống bộ khung của Task 10, cộng ba điểm riêng:
  - Lý thuyết phải dạy được ADT tách khỏi cài đặt: Map là hợp đồng, hash table là một cách cài, BST là cách cài khác. Đây là chỗ duy nhất trong chương trình dạy ý này, theo mục 5 của spec.
  - Phủ đủ: hàm băm, va chạm và hai cách xử lý, hệ số tải và resize, vì sao O(1) là trung bình chứ không phải xấu nhất.
  - Câu quiz `recall: true` phải hỏi về nhóm đã học trước đó trong Chương 3 hoặc Chương 1, không hỏi về chính bảng băm.
- `project`: bộ đếm tần suất từ, tự cài hash table, yêu cầu cuối bắc cầu sang so sánh với `unordered_map`.

- [ ] **Step 1: Viết file dữ liệu `src/data/lessons/bang-bam.js`**

Cùng khuôn Task 10 Step 1.

- [ ] **Step 2: Đăng ký vào `src/data/lessons/index.js`**

- [ ] **Step 3: Viết `src/sections/BangBam.vue`**

Sao khuôn từ `src/sections/DoPhucTap.vue` vừa viết ở Task 10, đổi `sid` và nội dung.

- [ ] **Step 4: Nối vào `src/App.vue`**

Thêm import và thẻ `<BangBam :active="activeSection === 'bang-bam'" />`, đặt ngay sau `<NganXepHangDoi ...>` để khớp thứ tự Chương 3.

- [ ] **Step 5: Bật cờ trong `parts.js`**

Đổi `bang-bam` thành `ready: true, duAn: true`.

- [ ] **Step 6: Chạy test và build**

Run: `npm run test -- --run` rồi `npm run build`
Expected: PASS.

- [ ] **Step 7: Kiểm chứng bằng mắt**

Run: `npm run dev`. Kiểm giống Task 10, thêm: code C++ dài không tràn ngang, bảng so sánh cuộn được trong khung riêng.

- [ ] **Step 8: Commit**

```bash
git add src/data/lessons src/sections/BangBam.vue src/App.vue src/lesson/parts.js
git commit -m "content: bai Bang bam, bai mau cho dang cau truc du lieu"
```

---

## Task 12: Chốt Giai đoạn 0–2

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/superpowers/specs/2026-08-09-mo-rong-30-nhom-design.md`
- Modify: `README.md`

- [ ] **Step 1: Chạy toàn bộ kiểm chứng**

```bash
npm run test -- --run
npm run build
```

Cả hai phải xanh. Ghi lại số test đã chạy để đưa vào nhật ký.

- [ ] **Step 2: Cập nhật spec cho khớp thực tế**

Sửa mục 6.1 và 7.3 của spec: `capstone` nằm ở `src/data/capstones/<chapter-key>.js`, không nằm trong dữ liệu bài cuối chương. Nêu đúng hai lý do đã ghi ở đầu kế hoạch này.

- [ ] **Step 3: Cập nhật `CLAUDE.md`**

Thay mục "Công việc đang chạy" bằng đợt mới: trỏ tới kế hoạch này và spec; ghi bảng model của Giai đoạn 0–5; ghi rõ `CHAPTERS` trong `parts.js` là nguồn sự thật duy nhất về thứ tự bài học và `nav.js` suy ra từ nó, cấm viết tay lại.

- [ ] **Step 4: Cập nhật `README.md`**

Ghi cấu trúc mới: 7 chương, 30 nhóm, khung 8 mục, cờ `ready`/`duAn`, thư mục `src/data/capstones/`.

- [ ] **Step 5: Ghi nhật ký phiên**

Thêm một dòng vào bảng "Nhật ký phiên làm việc" trong kế hoạch cũ `docs/superpowers/plans/2026-08-09-content-rewrite.md`, ghi rõ đợt mới bắt đầu và trỏ sang kế hoạch này.

- [ ] **Step 6: Commit**

```bash
git add CLAUDE.md README.md docs/superpowers
git commit -m "docs: chot Giai doan 0-2, cap nhat spec va huong dan du an"
```

- [ ] **Step 7: DỪNG LẠI — nhắc đổi model**

Không tự ý bước sang Giai đoạn 3. Báo với người dùng:

> Giai đoạn 0–2 đã xong, test và build xanh. Giai đoạn 3 là 18 bài lặp khuôn — theo bảng chi phí trong CLAUDE.md, phần này chạy bằng Sonnet qua subagent tuần tự, mỗi lần đúng một agent. Bạn đổi model bằng `/model` trước khi tôi bắt đầu nhé.

---

## Sau kế hoạch này

Giai đoạn 3–5 sẽ có kế hoạch riêng, mỗi chương một kế hoạch, viết khi tới lượt:

| Kế hoạch | Nội dung | Model |
|---|---|---|
| `...-ch1-nen-mong.md` | 3 bài còn lại Chương 1 | Sonnet, subagent tuần tự |
| `...-ch2-xu-ly-day.md` | 4 bài + MVP Ch2 | Sonnet |
| `...-ch3-tra-cuu.md` | 2 bài còn lại + MVP Ch3 | Sonnet |
| `...-ch4-cay.md` | 1 bài + MVP Ch4 | Sonnet |
| `...-ch5-do-thi.md` | 3 bài + MVP Ch5 | Sonnet |
| `...-ch6-thiet-ke.md` | MVP Ch6 | Sonnet |
| `...-ch7-chuyen-de.md` | 5 bài + MVP Ch7 | Sonnet |
| `...-gd4-bai-cu.md` | Bổ sung Phần 7 và quiz ôn cho 10 bài cũ | Sonnet |
| `...-gd5-ra-soat.md` | Rà soát 30 bài, giọng văn, giao diện, README | Opus |

Lý do chia nhỏ thay vì một kế hoạch khổng lồ: mỗi chương là một lát cắt giao được, và một kế hoạch 30 bài sẽ dài tới mức không ai đọc hết trước khi bắt tay làm.
